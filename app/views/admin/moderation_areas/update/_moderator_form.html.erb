<%= form_tag(update_moderator_admin_moderation_area_path(moderation_area), method: :patch, remote: true, id: "update-moderator-form-#{moderation_area.id}", class: "update-form") do %>
  <div class="btn btn-<%= moderation_area.moderator.try(:slack_id) ? "default" : "danger" %>">
    <%= select_tag 'moderator_id', options_for_select(moderators_for_select, moderation_area.moderator_id), include_blank: true, class: "form-control update-select" %>
  </div>
<% end %>
