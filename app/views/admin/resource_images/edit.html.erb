<div class="container">
  <ol class="breadcrumb">
    <li><%= link_to "Photos de contenus pédagogiques", admin_resource_images_path %></li>
    <li class="active">Modification d'une photo</li>
  </ol>

  <h2>
    <span class="text-muted">#<%= @resource_image.id %></span>
    <%= @resource_image.title %>
  </h2>

  <%= render partial: 'form' %>
</div>
