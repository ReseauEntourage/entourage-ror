<%= render 'nav', active: :neighborhoods %>

<div class="container">
  <div class="row justify-content-center">
    <div class="col-lg-8 col-12">
      <table class="table table-hover">
        <thead>
          <tr>
            <td colspan="2" class="text-right pr-0 pt-0 border-top-0">
              <a href="<%= new_community_admin_neighborhood_path %>"
                 class="btn btn-outline-secondary">Créer un voisinage de quartier</a>
            </td>
          </tr>
        </thead>
        <% @neighborhoods.each do |neighborhood| %>
          <tr class="js-overlay-container">
            <td>
              <%= link_to "", community_admin_neighborhood_path(neighborhood), class: "js-overlay" %>
              <%= neighborhood.title %>
            </td>
            <td>
              <% if neighborhood.has_pending %>
                <span class="badge badge-primary">En attente</span>
              <% end %>
            </td>
          </tr>
        <% end %>
      </table>
    </div>
  </div>
</div>
