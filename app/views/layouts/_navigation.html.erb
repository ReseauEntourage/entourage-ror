<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= image_tag('logo.png', alt:'Entourage', class:'navbar-brand') %>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <% if current_admin.present? %>
        <%= render partial: "layouts/switch_user" %>
      <% else %>
        <p class="nav navbar-nav navbar-left navbar-text"> Bonjour, <%= current_user.try(:first_name) %></p>
      <% end %>

      <ul class="nav navbar-nav navbar-right">
        <li class="<%= active_class(dashboard_organizations_path) %>"><%= link_to 'Dashboard', dashboard_organizations_path %></li>
        <li class="<%= active_class(edit_organization_path(current_user.organization)) %>"><%= link_to 'Gestion association', edit_organization_path(current_user.organization) %></li>
        <li class="<%= active_class(users_path) %>"><%= link_to 'Gestion utilisateurs', users_path %></li>
        <% if current_admin %>
          <li class="<%= active_class(registration_requests_path) %>"><%= link_to "Gestion des demandes d'adhésions", registration_requests_path %></li>
        <% end %>
        <li><%= link_to 'Gestion Profil', edit_user_path(current_user) %></li>
        <li><%= link_to 'Déconnexion', session_path(current_user), method: :delete %></li>
      </ul>
    </div>
  </div>
</nav>
